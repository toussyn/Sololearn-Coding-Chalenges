/*
The first two lines of the given code take two numbers as input.

Task
Complete the code to output whether or not the numbers are equal (true or false).

Sample Input
11
11

Sample Output
true
*/

var num1 = parseInt(readLine(), 10);
var num2 = parseInt(readLine(), 10);
  console.log(num1 === num2); 
